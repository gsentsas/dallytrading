/**
 * Dally Ops — configuration Next.

 * Volontairement pauvre. Cette application sert un entrepôt : elle n'a ni
 * images distantes, ni réécritures, ni intégrations tierces. Chaque option
 * ajoutée ici devra se justifier.
 */
const nextConfig = {
  reactStrictMode: true,
  poweredByHeader: false,
};

export default nextConfig;
