<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="0">

        <!--
            Multi-company isolation, global.

            global="True" means the rule is ANDed with every other rule rather than
            ORed with the group rules. Without it, a group rule granting access would
            override the company boundary — which is how one company's margins become
            visible to another's staff.
        -->
        <record id="rule_trade_opportunity_company" model="ir.rule">
            <field name="name">Trade opportunity — multi-company</field>
            <field name="model_id" ref="model_dally_trade_opportunity"/>
            <field name="global" eval="True"/>
            <field name="domain_force">
                ['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]
            </field>
        </record>

        <record id="rule_trade_line_company" model="ir.rule">
            <field name="name">Trade line — multi-company</field>
            <field name="model_id" ref="model_dally_trade_line"/>
            <field name="global" eval="True"/>
            <field name="domain_force">
                ['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]
            </field>
        </record>

        <record id="rule_trade_cost_company" model="ir.rule">
            <field name="name">Trade cost — multi-company</field>
            <field name="model_id" ref="model_dally_trade_cost"/>
            <field name="global" eval="True"/>
            <field name="domain_force">
                ['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]
            </field>
        </record>

        <record id="rule_trade_commission_company" model="ir.rule">
            <field name="name">Trade commission — multi-company</field>
            <field name="model_id" ref="model_dally_trade_commission"/>
            <field name="global" eval="True"/>
            <field name="domain_force">
                ['|', ('company_id', '=', False), ('company_id', 'in', company_ids)]
            </field>
        </record>

        <!--
            The API user sees only what it created.

            Least privilege applied to reading, not just writing. Without this rule a
            leaked API key could enumerate every trade deal in the database — including
            those created by staff, with their internal notes and their counterparties.
            With it, the blast radius of a leaked key is the records that key itself
            created.
        -->
        <record id="rule_trade_opportunity_api_own_records" model="ir.rule">
            <field name="name">Trade opportunity — API user sees only its own records</field>
            <field name="model_id" ref="model_dally_trade_opportunity"/>
            <field name="groups" eval="[(4, ref('dally_trade.group_dally_trade_api'))]"/>
            <field name="domain_force">[('create_uid', '=', user.id)]</field>
            <field name="perm_read" eval="True"/>
            <field name="perm_write" eval="True"/>
            <field name="perm_create" eval="True"/>
            <field name="perm_unlink" eval="True"/>
        </record>

    </data>
</odoo>
